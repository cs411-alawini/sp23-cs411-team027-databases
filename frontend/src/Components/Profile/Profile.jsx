
export default function Profile(props) {
    console.log(props)
    // <tbody>
    //   {items.map(item => <ObjectRow key={item.id} name={item.name} />)} 
    // </tbody>
    return (
      <div>Profile
     
      </div>
    )
}